jQuery(document).ready(function(){
    let maxHeight = Math.max.apply(Math, jQuery("#relatedPost .post").map(function () {
        return jQuery(this).outerHeight(); 
    }));
    
    
    jQuery("#relatedPost .post").each(function() {
        console.log(jQuery(this).attr("style","height:300px !important;"));
    });
    
    console.log(maxHeight);
    
    
});

